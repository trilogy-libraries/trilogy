# MariaDB 11.8 macOS config
# Appended to $(brew --prefix)/etc/my.cnf in CI

[mysqld]
# MariaDB 11.4+ disabled performance_schema by default
# Tests need it for SSL status queries
performance_schema = ON
